import { FilterForm } from 'components/FilterForm/FilterForm';

import './Sidebar.scss';

export const Sidebar = () => {
  return (
    <aside className="Sidebar">
      <FilterForm />
    </aside>
  );
};
